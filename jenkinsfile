pipeline {
    agent any
    tools {
        maven 'maven123'
    }
    stages {
        stage('Get maven version') {
            steps {
                sh 'mvn --version'
            }
        }
    }
}